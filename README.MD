# Password Generator

This is project is a password generator that allows users to generate a random password with specified parameters.

This project also include the additional enhancement of forcing constrictions on the rules that the password has to follow, such as needing at least one number or one symbol.